# Task: Plan OAuth2 Implementation

We need to add "Login with Google" and "Login with GitHub" to our web application.

Your task:
Create a file named `todo.md` that contains the implementation plan.

Requirements:
1. List the necessary configuration variables (Client ID, etc.).
2. Describe the flow: Redirect -> Callback -> Token Exchange -> User Session.
3. List the library/dependencies you would use (e.g., Passport.js, Authlib).
4. Include steps for secure storage of credentials.
