# T\u00e2che : Remplacer les prints par une librairie de logging

Le fichier `app.py` utilise des `print` pour le d\u00e9bogage.
Refactorise le code pour utiliser la librairie standard `logging`.
Configure `logging` pour afficher les messages avec un format simple : `[LOG] message`.
Remplace tous les `print` par des appels appropri\u00e9s (ex: `logging.info`).
