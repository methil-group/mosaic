# Task: Update Status Enum in Gleam

1. Open `src/logic_update.gleam`.
2. Add a new variant `Archived` to the `Status` type.
3. Update the `to_string` function to handle the `Archived` variant by returning `"archived"`.
