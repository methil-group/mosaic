/**
 * Implémenter l'algorithme de recherche de chemin A* sur une grille 2D.
 * 
 * @param {number[][]} grille - Tableau 2D où 0 est marchable et 1 est un obstacle.
 * @param {number[]} depart - [ligne, col]
 * @param {number[]} destination - [ligne, col]
 * @returns {number[][]} - Chemin sous forme de coordonnées: [[l1, c1], [l2, c2], ...]
 */
function trouverChemin(grille, depart, destination) {
    // TÂCHE : Implémenter la logique ici.
    return [];
}

module.exports = { trouverChemin };
