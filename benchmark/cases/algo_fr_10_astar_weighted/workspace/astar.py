import math

def trouver_chemin_pondere(grille, depart, destination):
    """
    Trouver le chemin le plus court sur une grille pondérée.
    
    :param grille: Tableau 2D où chaque cellule contient le coût d'entrée (>= 1).
                   Un coût de float('inf') signifie un obstacle.
    :param depart: (ligne, col)
    :param destination: (ligne, col)
    :return: Liste de coordonnées (ligne, col) représentant le chemin.
    """
    # TÂCHE : Implémenter A* avec gestion des coûts.
    return []
