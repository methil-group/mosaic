# Fix the Calculator

The file `calc.py` has a bug where it crashes on division by zero.
There is a test suite in `test_calc.py`.

Your task:
1. Run the tests to confirm the failure.
2. Fix `calc.py` so that `divide(a, 0)` returns `None`.
3. Run the tests again to ensure everything passes.
