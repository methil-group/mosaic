# Task: Refactor Node.js Callbacks to Async/Await

Modify `script.js` to use `fs.promises` and an `async` function instead of the callback-based `fs.readFile`.
The function should be named `readFileAsync` and called at the end of the script.
It must still log the data to the console.
