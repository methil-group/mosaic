import math

def find_path_weighted(grid, start, end):
    """
    Find the shortest path on a weighted grid.
    
    :param grid: 2D list where each cell contains the cost to enter that cell (>= 1).
                 A cost of float('inf') means the cell is an obstacle.
    :param start: (row, col)
    :param end: (row, col)
    :return: List of (row, col) coordinates representing the path.
    """
    # TASK: Implement A* with cost weights.
    return []
