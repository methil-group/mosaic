# Task: OOP Inheritance Refactor

Refactor the procedural `calculate_area` logic in `shapes.py` into a clean object-oriented hierarchy.

Requirements:
- Ensure `Circle` and `Rectangle` inherit from `Shape`.
- Both subclasses must implement the `area()` method.
- `Circle` should take `radius` in its `__init__`.
- `Rectangle` should take `width` and `height` in its `__init__`.
