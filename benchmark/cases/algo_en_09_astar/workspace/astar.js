/**
 * Implement the A* pathfinding algorithm on a 2D grid.
 * 
 * @param {number[][]} grid - 2D array where 0 is walkable and 1 is an obstacle.
 * @param {number[]} start - [row, col]
 * @param {number[]} end - [row, col]
 * @returns {number[][]} - Array of coordinates representing the path: [[r1, c1], [r2, c2], ...]
 */
function findPath(grid, start, end) {
    // TASK: Implementation goes here.
    return [];
}

module.exports = { findPath };
